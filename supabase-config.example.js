/* Committee Tape — Supabase connection config template.
 *
 * Copy this file to `supabase-config.js` and fill in your values.
 * `supabase-config.js` is git-ignored, so real credentials never get
 * committed. See the README → Config section.
 *
 * Grab SUPABASE_URL and SUPABASE_ANON_KEY (the anon/publishable key)
 * from Supabase Dashboard → Settings → API. The anon key is public by
 * design — never put your service_role/secret key in this file.
 *
 * If your host injects these as global variables (e.g. an inline env
 * script from Netlify/Vercel), those take precedence over the
 * placeholders here.
 */
window.SUPABASE_URL = window.SUPABASE_URL || 'PASTE_YOUR_SUPABASE_URL_HERE';
window.SUPABASE_ANON_KEY = window.SUPABASE_ANON_KEY || 'PASTE_YOUR_SUPABASE_ANON_KEY_HERE';
